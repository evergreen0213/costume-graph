# 버전을 정확히 고정한다.
#
# viewdata.py 의 좌표·보로노이 계산은 numpy/scipy 버전에 따라 결과가 미세하게
# 달라진다. 실제로 최신 조합(numpy 2.4 / scipy 1.17)에서는 바느질선이 328개 ->
# 333개로, span 이 1553.4 -> 1553.5 로 바뀌었다. 군집과 키워드 구성은 같았지만
# 지도의 조각 모양이 달라진다.
#
# 매달 자동으로 도는 파이프라인이라, 눈으로 확인하고 승인한 지도가 그대로
# 배포되어야 한다. 고정하지 않으면 pip 가 새 버전을 물어오는 달에 아무도
# 건드리지 않은 지도가 혼자 바뀐다.
#
# 파이썬 버전은 영향이 없다 (3.9 / 3.11 에서 같은 결과 확인).
# 버전을 올릴 때는 지도가 바뀔 수 있으므로 눈으로 한 번 보고 올린다.

pandas==2.3.3
xlrd==2.0.2        # .xls (구형 엑셀) 판독용
networkx==3.2.1    # viewdata.py 군집 분석
numpy==2.0.2       # viewdata.py 좌표 계산
scipy==1.13.1      # viewdata.py 보로노이 분할 + 거리장(ndimage)
shapely==2.0.7     # viewdata.py 한반도 윤곽으로 조각 잘라내기 (오목 다각형)
matplotlib==3.9.4  # viewdata.py 국토 안팎 판정 (Path.contains_points). 그리기용 아님
