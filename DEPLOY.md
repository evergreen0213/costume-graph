# 배포와 자동 최신화

맥을 켜둘 필요가 없다. 깃허브가 새 호에 맞춰 알아서 돌린다.

```
GitHub 저장소 ──(짝수달 1일 09:00 KST, Actions)──▶ build/update.sh
                                                       │
                                             costume-graph.html
                                                       │
                                                 GitHub Pages
                                                       │
       학회 홈페이지 커스텀 페이지 ◀── <iframe src="…"> 한 줄
```

## 두 가지 길 — 먼저 고르고 시작한다

| | A. Pages + iframe | **B. 자기 갱신 파일** | C. 그냥 파일 |
|---|---|---|---|
| 최신화 | 새 호마다 자동 | **새 호마다 자동** | 멈춘다 |
| 담당자가 할 일 | iframe 한 줄 (1회) | **업로드 1회, 이후 없음** | 새 파일마다 재업로드 |
| 주소 | `github.io` | **학회 도메인** | 학회 도메인 |
| 바깥 의존 | 열람 시 github.io | 열람 시 github.io (실패해도 뜸) | 없음 |

**B 를 쓴다.** 파일은 학회 도메인 안에 있으면서 자료는 새 호가 나올 때마다 알아서 받아 온다.
아래 '자기 갱신' 항목이 그 방식이다. A 는 그 자료를 올려 두는 자리로 함께 쓴다.

## 담당자에게 파일로 넘길 때 (B)

`costume-graph.html` **한 파일이면 끝난다.** 이미지·자료·글꼴 설정까지 전부 파일 안에
들어 있어서 같이 보낼 것이 없다. (실제로 부르는 바깥 주소는 구글 폰트 하나뿐이고,
그마저 막혀도 시스템 글꼴로 대체돼 정상 동작한다.)

**다만 넘기는 방법이 중요하다.**

> ### ❌ 이렇게 하면 깨진다
> 관리자 화면의 **글쓰기·본문 편집기에 붙여넣기**. 국내 홈페이지 솔루션 대부분이
> 저장할 때 `<script>` 와 `<style>` 을 보안상 지워 버린다. 지워지면 지도는 아무것도
> 그려지지 않는다. 파일이 581 KB 라 본문 글자수 제한에도 걸린다.
>
> ### ✅ 이렇게 넘긴다
> **파일 그대로 웹서버에 정적 파일로 업로드.** 편집기를 거치지 않는다.

아래를 그대로 담당자에게 보내면 된다.

---

> 안녕하세요. 학회 창립 50주년 기념 『복식 연구 지도』 페이지 파일을 보냅니다.
>
> - 보내는 것: `costume-graph.html` 파일 **하나** (약 580 KB)
> - 이미지나 CSS 등 **따로 올릴 파일은 없습니다.** 전부 이 파일 안에 들어 있습니다.
>
> 이 파일은 **한 번만 올리시면 됩니다.** 새 논문이 실리면 페이지가 스스로 자료를
> 받아 오도록 만들어 두었습니다. 다시 보내 드릴 일은 없습니다.
>
> **부탁드리는 작업**
>
> 1. 이 파일을 **수정하지 말고 그대로** 웹서버에 올려 주세요.
>    (예: `https://학회도메인/costume-graph/index.html`)
> 2. 게시판이나 관리자 **본문 편집기에 붙여넣지 말아 주세요.** 편집기가 저장할 때
>    스크립트를 지워서 화면이 비어 보입니다. 파일 업로드(FTP 등)로 부탁드립니다.
> 3. FTP 로 올리실 때는 **바이너리 모드**로 부탁드립니다. 텍스트 모드로 올리면
>    한글이 깨질 수 있습니다.
> 4. 올리신 뒤 주소를 알려 주시면 확인하겠습니다.
>
> **홈페이지 안에 넣어 보이고 싶으시면** 커스텀 페이지에 아래 한 줄만 넣으시면 됩니다.
> (지금 `/homepage/custom/search` 가 DBpia 를 넣는 방식과 같습니다.)
>
> ```html
> <IFRAME src="https://학회도메인/costume-graph/" width="100%" height="900"
>         frameBorder="0" style="border:0;display:block"
>         title="복식 연구 지도"></IFRAME>
> ```
>
> 페이지가 https 라면 iframe 주소도 https 여야 합니다. 높이는 900px 이상을 권합니다.

---

### 넘기기 전에 스스로 확인할 것

```bash
node build/smoke.js costume-graph.html     # 통과해야 한다
open costume-graph.html                    # 브라우저에서 실제로 눌러 본다
```

## 처음 한 번만 하는 설정

### 1. 깃허브 로그인

터미널에서 (Claude Code 안이라면 앞에 `!` 를 붙여서):

```bash
gh auth login
```

### 2. 저장소 만들고 올리기

```bash
gh repo create costume-graph --public --source=. --remote=origin --push
```

> **공개(`--public`) / 비공개(`--private`)**
> Pages 를 무료로 쓰려면 공개 저장소여야 한다. 비공개로 두려면 GitHub Pro 이상이 필요하다.
> 공개로 두면 `한국복식학회/excel/*.xls` 안의 **초록 전문까지 함께 공개**된다.
> 어차피 KCI·DBpia 에서 열람되는 내용이고 학회가 저작권자이지만, 꺼려진다면
> `.gitignore` 에 `한국복식학회/ris/` 와 초록 열을 덜어낸 사본을 쓰는 쪽으로 바꾸면 된다.

### 3. Pages 켜기

저장소 → **Settings → Pages → Source** 를 **GitHub Actions** 로 바꾼다.
(기본값인 "Deploy from a branch" 로 두면 워크플로가 배포에 실패한다.)

### 4. 첫 배포 확인

**Actions** 탭 → `복식 서지 최신화` → **Run workflow** 로 손수 한 번 돌린다.
끝나면 주소가 나온다:

```
https://<계정명>.github.io/costume-graph/
```

### 5. 학회 홈페이지에 붙이기

관리자에서 커스텀 페이지를 하나 만들고 아래 한 줄을 넣는다.
지금 `/homepage/custom/search` 페이지가 DBpia 를 이런 식으로 넣고 있으므로 같은 방법이다.

```html
<IFRAME src="https://<계정명>.github.io/costume-graph/"
        width="100%" height="900" frameBorder="0"
        style="border:0;display:block"
        title="복식 연구 지도"></IFRAME>
```

이 뒤로는 손댈 일이 없다. 새 호가 나오면 알아서 들어가고,
iframe 주소는 그대로라 홈페이지는 건드리지 않아도 된다.

## 평소에 벌어지는 일

| 시점 | 동작 |
|---|---|
| 짝수달 1일 09:00 KST | Actions 가 `update.sh` 실행 (본 일정) |
| 홀수달 1일 09:00 KST | 같은 일을 하되, 대개 새 논문이 없어 그대로 끝난다 (안전선) |
| 새 논문 없음 | "새 논문 없음" 찍고 그대로 종료. 커밋도 배포도 없음 |
| 새 논문 있음 | 수집 → 정제 → 도상 → 화면 데이터 → 렌더 → **연기 시험** → 커밋 → Pages 배포 |
| 배포되는 안 | 하나뿐. `update.sh` 의 `DESIGN` 기본값이 정한다 |
| 연기 시험 실패 | 거기서 멈춘다. 커밋도 배포도 없음 — 죽은 페이지가 나가지 않는다 |

`build/**` 나 엑셀을 고쳐서 push 해도 다시 빌드된다.
급할 땐 Actions 탭에서 **Run workflow** 로 즉시 돌릴 수 있다.

## 왜 짝수달 1일인가 — 쫓아가지 않고 줍는다

『복식』은 격월간이고 발행일이 짝수달 말일(2/28·4/30·…·12/31)로 정해져 있다.
그런데 **실제 발행이 다음 달까지 밀리기도 한다.** 76권 4호가 그랬다 — 8월 31일자인데
9월 10일쯤 나왔다.

발행일에 맞춰 쫓아가면 이런 달에 한 회차를 통째로 놓친다. 그래서 쫓아가지 않는다.
**다음 발행월 1일에 가서 지난 호를 줍는다.**

```
 8/31자 호        10/31자 호        12/31자 호      ← 이름상 발행일
   ↓ (밀리면 9월 중순)  ↓                 ↓
      10/1              12/1              2/1        ← 받으러 가는 날
      └─ 한 달 남짓 여유. 늦어져도 그 안에 들어온다
```

한 달 여유를 두는 대신 반영이 그만큼 늦지만, **놓치는 회차가 없다.** 색인(CrossRef DOI
등록)까지 걸리는 시간도 이 여유 안에 함께 흡수된다.

### 홀수달에도 한 번 더 도는 이유 — 60일 규칙

깃허브는 **저장소에 60일간 활동이 없으면 예약 실행을 자동으로 꺼버린다.** 짝수달
1일끼리의 간격은 59~62일이라 대부분 60일을 넘는다.

```
10/1 → 12/1  61일   ← 걸린다
12/1 →  2/1  62일   ← 걸린다
 2/1 →  4/1  59일
 4/1 →  6/1  61일   ← 걸린다
```

즉 **짝수달만 걸어 두면 다음 실행 전에 예약이 꺼진다.** 그래서 홀수달 1일에도 한 번
깨우는 줄을 넣었다. 그날은 대개 새 논문이 없어 그대로 끝나지만, 저장소가 살아 있다는
표시는 남는다.

### 수집이 조용히 멎는 것을 막는다

예전에는 수집이 어떤 이유로 실패해도 "새 논문 없음" 으로 뭉개고 넘어갔다. 그러면 DBpia
쪽이 바뀌어 수집이 멎어도 로그를 들여다보지 않는 한 몇 달을 모른 채 지나간다. 이제는 가른다.

| 상황 | 동작 |
|---|---|
| 새 논문 없음 | 조용히 통과 |
| 새 논문을 받아 옴 | 계속 진행 |
| 그물망 오류 등 (CrossRef 접속 실패) | `::warning::` 남기고 기존 자료로 계속 |
| **새 논문을 찾았는데 하나도 못 받음** | `::error::` 내고 **거기서 멈춘다** → Actions 가 메일로 알린다 |

## 짝수달에도 한 번 더 도는 이유 — 60일 규칙

깃허브는 **저장소에 60일간 활동이 없으면 예약 실행을 자동으로 꺼버린다.** 그런데 홀수달
10일끼리의 간격은 59~62일이라 대부분 60일을 넘는다.

```
9/10 → 11/10  61일   ← 걸린다
11/10 → 1/10  61일   ← 걸린다
1/10 → 3/10   59일
3/10 → 5/10   61일   ← 걸린다
```

즉 **홀수달만 걸어 두면 다음 실행 전에 예약이 꺼진다.** 그래서 짝수달 10일에도 한 번
깨우는 줄을 넣었다. 그날은 대개 새 논문이 없어(발행일이 그달 말일이다) 아무것도 하지 않고
끝나지만, 저장소가 살아 있다는 표시는 남는다.

## 라이브러리 버전을 고정해 둔 이유

`requirements.txt` 는 `==` 로 정확히 고정돼 있다. 느슨하게 두면 안 되는 이유가 있다.

`viewdata.py` 의 좌표·보로노이 계산 결과가 numpy/scipy 버전에 따라 미세하게 달라진다.
실제로 최신 조합(numpy 2.4 / scipy 1.17)에서는 바느질선이 328 → 333개로, 테두리 반지름이
1553.4 → 1553.5 로 바뀌었다. 군집과 키워드 구성은 같았지만 조각 모양이 달라진다.

고정하지 않으면, 아무도 손대지 않은 달에 pip 가 새 버전을 물어와 지도가 혼자 바뀐다.
눈으로 보고 승인한 지도가 그대로 배포되도록 고정해 둔 것이다.

파이썬 버전은 영향이 없다 (3.9 / 3.11 에서 산출물이 바이트 단위로 같음을 확인했다).
버전을 올릴 때는 지도가 바뀔 수 있으니 손으로 한 번 돌려 눈으로 확인하고 올린다.

**손으로 돌릴 때도 같은 버전을 쓰는 것이 좋다.** 안 그러면 화면에서 보는 지도와
배포되는 지도가 미세하게 달라진다.

```bash
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/bin/python -c "import sys; print(sys.version)"
PATH=".venv/bin:$PATH" bash build/update.sh
```

## 알아둘 함정: 60일 규칙

깃허브는 **저장소에 60일간 아무 활동이 없으면 예약 실행(schedule)을 자동으로 꺼버린다.**
『복식』이 격월간이라 "새 호가 없어서 커밋도 없는" 기간이 길어지면 걸릴 수 있다.

꺼지기 전에 저장소 관리자에게 메일이 오고, Actions 탭에서 **Enable workflow** 버튼
한 번이면 되살아난다. 메일을 놓치지 않도록 저장소 알림을 켜두면 된다.

## 손으로 돌리기

```bash
pip install -r requirements.txt
bash build/update.sh
```

자세한 내부 동작은 `build/README.md` 참고.

## 브라우저 지원 범위

**사파리에서 지도가 반만 그려진다는 제보가 있었고, 원인을 찾아 고쳤다.**

캔버스의 `roundRect` 는 **사파리 16(2022년 9월)부터** 생긴 기능이다. 그 이전 사파리에서는
갈래 이름표를 그리는 자리에서 예외가 나면서 `draw()` 가 중간에 멈췄다. 게다가 실패한
프레임을 계속 다시 그리는 구조라 초당 60번씩 같은 예외를 되풀이했다 — 지도는 덜 그려지고
노트북은 뜨거워진다. 정확히 "사파리에서 안 된다"고 할 만한 증상이다.

고친 방법은 두 가지다.

1. `roundRect` 가 없으면 `arcTo` 로 손수 그리는 폴리필을 넣었다.
2. 그리기 루프가 한 프레임 실패해도 되풀이하지 않게 했다. 앞으로 다른 원인으로 한 프레임이
   실패하더라도 페이지 전체가 마비되지는 않는다.

`build/smoke.js` 는 `roundRect` 가 없는 캔버스를 흉내 내므로, 이 폴리필이 사라지면
연기 시험이 잡아낸다.

| 브라우저 | 상태 |
|---|---|
| 크롬 · 엣지 · 파이어폭스 (최신) | 확인함 |
| 사파리 16+ / iOS 16+ | 정상 |
| 사파리 14.1~15 / iOS 14.5~15 | 동작함. 위 폴리필로 지도는 온전히 그려진다 |
| 사파리 14.0 이하 | 여백(flex `gap`)이 무너져 좁아 보인다. 지도 자체는 나온다 |
| 인터넷 익스플로러 | 지원하지 않는다 (MS 도 2022년에 지원을 끝냈다) |

`text-wrap:pretty`, `overscroll-behavior`, `:focus-visible` 은 낡은 사파리에 없지만
전부 겉모습에만 관계된 것이라 없어도 기능은 같다.

## 자기 갱신 — 파일을 넘겨도 자동으로 새로워지는 방식

담당자에게 파일을 넘기면 최신화가 멈추는 것이 원래 문제였다. 그래서 **넘긴 파일이
스스로 새 자료를 받아 오게** 했다. 담당자는 처음 한 번만 올리면 된다.

```
[학회 도메인]  costume-graph.html   ← 담당자가 딱 한 번 올린다
                    │
                    │ ① 품고 있는 자료로 먼저 그린다 (네트워크를 기다리지 않는다)
                    │
                    │ ② 조용히 판만 물어본다
                    ▼
[GitHub Pages] costume-graph.version.json   {"build":"a27a64…"}   60바이트쯤
                    │
                    │ ③ 판이 다르면 그때만 자료를 받는다
                    ▼
               costume-graph.data.json      476 KB
                    │
                    └─▶ 새 자료로 다시 그린다. 판 표시에 날짜가 붙는다.
```

### 지켜지는 것

| 상황 | 결과 |
|---|---|
| 평소 (판 그대로) | 60바이트만 오간다. 476 KB 는 받지 않는다 |
| GitHub 이 막혀 있거나 죽음 | 품고 있던 자료로 그대로 뜬다. 사용자는 모른다 |
| 자료가 깨져 있음 | 모양을 검사해서 버린다. 품은 자료로 남는다 |
| 다른 안(디자인)의 자료가 올라옴 | `design` 이 다르면 거부한다 |
| 사용자가 이미 대문을 지나 지도를 보는 중 | **갈아끼우지 않는다.** 보던 화면을 되돌리지 않는다 |
| 낡은 브라우저 (fetch 없음) | 갱신을 건너뛴다. 지도는 정상 |

`build` 는 날짜가 아니라 **자료 내용의 해시**다. 아무것도 바뀌지 않은 달에는 값이 그대로라,
보는 사람이 476 KB 를 괜히 다시 받지 않는다.

### 켜는 법

`render.py` 가 `PAGES_URL` 환경변수를 파일에 박는다. 비어 있으면 자기 갱신을 하지 않는다.

```bash
PAGES_URL=https://<계정>.github.io/costume-graph bash build/update.sh
```

Actions 에서는 저장소의 기본 Pages 주소를 스스로 짓는다. 다른 주소에 올린다면
저장소 **Settings → Secrets and variables → Actions → Variables** 에 `PAGES_URL` 을 넣는다.

CI 의 '산출물 점검' 단계가 주소가 실제로 박혔는지, 판 번호가 두 파일에서 일치하는지 확인한다.
비어 있으면 배포를 멈춘다 — 자기 갱신이 조용히 꺼진 채 나가는 사고를 막으려는 것이다.

### Pages 에 함께 올라가는 파일

```
site/index.html                       ← 그대로 열어도 되는 주소
site/costume-graph.html               ← 담당자에게 넘길 파일 (같은 내용)
site/costume-graph.data.json          ← 넘긴 파일이 받아 갈 자료
site/costume-graph.version.json       ← 판 번호
```

**이 두 json 의 이름은 바꾸면 안 된다.** 이미 학회 서버에 올라간 파일이 이 이름으로 찾는다.

## 문서 뼈대 — 왜 `<!DOCTYPE>` 과 charset 이 필요했나

예전에는 파일이 `<title>` 로 바로 시작했다. 로컬에서 열 때는 멀쩡했지만, 실제로 서버에
올려 보니 문제가 셋이었다.

1. **`<meta charset>` 없음** — 서버가 `Content-Type` 에 `charset=utf-8` 을 안 붙이면
   브라우저가 인코딩을 짐작한다. 한국어 환경에서는 EUC-KR 로 짚어 **한글이 전부 깨진다.**
   학회 서버 설정을 우리가 정할 수 없으므로 파일이 스스로 밝혀야 한다.
   charset 은 **문서 앞 1024바이트 안**에 있어야 읽힌다.
2. **`<!DOCTYPE html>` 없음** — 브라우저가 쿼크스 모드로 그린다. 상자 계산이 달라진다.
3. **viewport meta 없음** — 휴대폰이 980px 화면인 척하고 축소해 버려서,
   `@media (max-width:880px)` 로 짜 둔 반응형이 아예 걸리지 않았다.

셋 다 넣었다. 확인은 `document.compatMode` 가 `CSS1Compat` 인지, `document.characterSet` 이
`UTF-8` 인지 보면 된다.
